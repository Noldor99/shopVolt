import type { FilterConfig } from '@/types/filter'

// Filters are now fully server-driven; keep empty exports for compatibility.
export const TABLET_FILTERS: FilterConfig[] = []
export const MONITOR_FILTERS: FilterConfig[] = []
